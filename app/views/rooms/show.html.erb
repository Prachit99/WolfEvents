<p id="notice"><%= notice %></p>

<p>
  <strong>Room loc:</strong>
  <%= @room.room_loc %>
</p>

<p>
  <strong>Room cap:</strong>
  <%= @room.room_cap %>
</p>

<%= link_to 'Edit', edit_room_path(@room) %> |
<%= link_to 'Back', rooms_path %>

<p id="notice"><%= notice %></p>

<h1>Event Tickets</h1>

<table>
  <thead>
    <tr>
      <th>Confirmation num</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @event_tickets.each do |event_ticket| %>
      <tr>
        <td><%= event_ticket.confirmation_num %></td>
        <td><%= link_to 'Show', event_ticket %></td>
        <td><%= link_to 'Edit', edit_event_ticket_path(event_ticket) %></td>
        <td><%= link_to 'Destroy', event_ticket, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event Ticket', new_event_ticket_path %>
